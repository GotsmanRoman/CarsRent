import styles from './RentButton.module.scss';

const Modal = () => {
  return (
    <a href="tel:+380730000000" className={styles.rent__button}>
      Rental Car
    </a>
  );
};

export default Modal;
